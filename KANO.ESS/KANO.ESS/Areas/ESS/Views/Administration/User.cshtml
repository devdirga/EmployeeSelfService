@using KANO.Core.Model
@using KANO.Core.Service
@using KANO.ESS
@using Newtonsoft.Json;
@{
    Layout = "_Layout";
}

<script>
    const URL_GET_USER_MANAGEMENT = "@Url.Action("GetData", "User")";
    const URL_GET_DETAILS_USER_MANAGEMENT = "@Url.Action("GetDetail", "User")";
    const URL_SAVE_USER = "@Url.Action("Save", "User")"
    const URL_GET_GROUP_MANAGEMENT = "@Url.Action("GetData", "Group")";
    model.proto = {
        user:@Html.Raw(JsonConvert.SerializeObject(new User(), Formatting.Indented)),

    }
</script>
<script src="~/assets/areas/ess/js/usermanagement.js?@(Startup.Tick??DateTime.Now.Ticks)"></script>

<style>
    span.k-widget.k-dropdown.form-control {
        width: 100%;
    }
</style>

<div class="row">
    <div class="col-md-12">
        <div class="card box">
            <div class="card-header clearfix">
                <a href="#" onclick="model.form.Add()" class="btn btn-sm btn-info float-md-right w-md-xs-100">Create</a>
            </div>
            <div class="card-body">
                <div id="gridMain"></div>
            </div>
        </div>
    </div>
</div>

@await Html.PartialAsync("ModalUser.cshtml")

<script>
    $(function () {
        @*Render UI Components*@
        model.form.GetDataGrid();
       // model.form.getFilter();
    })
</script>