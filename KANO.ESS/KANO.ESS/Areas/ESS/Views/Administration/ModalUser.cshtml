

<div id="modalUserManagement" class="modal" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    <i class="mdi mdi-account"></i>
                    User
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label>Username</label>
                        <input type="text" disabled placeholder="Name" class="form-control" data-bind="value: model.form.DetailData().Username, enable: model.form.action() == 'new'" />
                    </div>
                    <div class="form-group">
                        <label class="required">Full Name</label>
                        <input type="text" disabled placeholder="Full Name" class="form-control" data-bind="value: model.form.DetailData().FullName, enable: model.form.action() == 'edit' || model.form.action() == 'new'" />
                    </div>
                    <div class="form-group">
                        <label class="required">Email</label>
                        <input type="text" placeholder="Email" class="form-control" data-bind="value: model.form.DetailData().Email, enable: model.form.action() == 'edit' || model.form.action() == 'new'" />
                        <small class="form-text text-muted mb-2">
                            This will only change user email in internal ESS not in AX Employee
                        </small>
                    </div>
                    <div class="form-group">
                        <label data-bind="css:{required:model.form.action() == 'new'}">Password</label>
                        <input type="password" disabled placeholder="Password" class="form-control" data-bind="value: model.form.DetailData().Password, enable: model.form.action() == 'edit' || model.form.action() == 'new'" />
                        <small class="form-text text-muted mb-2" data-bind="if:model.form.action() != 'new'">
                            Let it empty, if you are not willing to change the password
                        </small>
                    </div>
                    <div class="form-group">
                        <label class="required">Group</label>
                        <input type="text" id="GroupId" class="form-control" name="menu-GroupId" required validationMessage="Input Icon" data-bind="value: model.form.GroupId" />
                    </div>
                    <div class="form-check form-check-flat form-check-info">
                        <label class="form-check-label">
                            <input type="checkbox" class="form-check-input" id="Enable" data-bind="checked: model.form.DetailData().Enable"> Enable <i class="input-helper"></i>
                        </label>
                    </div>
                </form>
            </div>
            <div data-bind="visible: model.form.action() == 'edit' || model.form.action() == 'new'" class="modal-footer">
                <button type="button" class="btn btn-light" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-info" data-bind="click: model.form.Save">Save</button>
            </div>
        </div>
    </div>
</div>
